% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_ml.R
\name{fitBestModel}
\alias{fitBestModel}
\title{fitBestModel()}
\usage{
fitBestModel(final_mod, train_data)
}
\arguments{
\item{final_mod}{Best model workflow (output of \code{selectBestModel()})}

\item{train_data}{The part of the pangenome data designated for ML model
training. This can be the output of
\code{rsample::training(splitMLInputTibble(ml_input_tibble))}.}
}
\value{
Best model fit
}
\description{
Fits the best model (output of \code{selectBestModel()}).
}
\examples{
data(demo_ml_tibble)
data_split <- splitMLInputTibble(demo_ml_tibble, split = c(1, 0), seed = 1)
train <- rsample::training(data_split)
wflow <- buildWflow(buildLRModel(), buildRecipe(train))
set.seed(1)
tune_res   <- tuneGrid(wflow, data_split,
                       buildTuningGrid("LR", 10^c(-3, -1), c(0, 0.5, 1)),
                       n_fold = 2)
best_wflow <- selectBestModel(tune_res, wflow, "mcc")
fitBestModel(best_wflow, train)
}
