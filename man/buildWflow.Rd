% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_ml.R
\name{buildWflow}
\alias{buildWflow}
\title{buildWflow()}
\usage{
buildWflow(parsnip_mod, recipe)
}
\arguments{
\item{parsnip_mod}{A \code{parsnip} model object, such as the output of
\code{buildLRModel()} (random forest and boosted tree support planned)}

\item{recipe}{A recipe, such as the output of \code{buildRecipe()}}
}
\value{
A \code{workflow} object
}
\description{
Builds a \code{tidymodels} workflow based on an input model and recipe.
}
\examples{
train <- tibble::tibble(
  genome_id = paste0("g", 1:10),
  genome_drug.resistant_phenotype = rep(c("Resistant", "Susceptible"), each = 5),
  feat_a = rep(c(0L, 1L), 5),
  feat_b = rep(c(1L, 0L), 5)
)
rec <- buildRecipe(train, use_pca = FALSE)
lr  <- buildLRModel()
buildWflow(lr, rec)
}
